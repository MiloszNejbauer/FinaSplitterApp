import { Text, View } from 'react-native';

export default function LoginPlaceholder() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>To jest zapasowy plik logowania</Text>
    </View>
  );
}